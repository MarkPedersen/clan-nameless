#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../docco-plus/bin/docco-plus" "$@"
  ret=$?
else 
  node  "$basedir/../docco-plus/bin/docco-plus" "$@"
  ret=$?
fi
exit $ret
